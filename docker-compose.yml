version: "2"
services:
  crawl-web:
    build: .
    image: ca-multi-server:latest
    ports:
      - 9090:8080
    volumes:
      - crawl-web-data:/data
    restart: unless-stopped

volumes:
  crawl-web-data:
